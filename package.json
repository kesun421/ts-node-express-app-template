{
  "name": "ts-express-app-template",
  "version": "1.0.0",
  "description": "Template for Node + Typescript + Express app.",
  "main": "dist/main.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ke Sun",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^12.12.17",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
